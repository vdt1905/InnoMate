# Copy to .env. Vite only exposes variables prefixed with VITE_.

# Backend API root. Must include /api. The socket URL is derived from this by
# stripping the /api suffix, so keep them on the same host.
VITE_API_BASE_URL=http://localhost:5000/api
